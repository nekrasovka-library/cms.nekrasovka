import styled from "styled-components";

const BackToConstructorButton = styled.button`
  background-color: rgba(0, 0, 0, 0.95);
  border-radius: 40px;
  color: #fff;
  font-size: 16px;
  font-weight: 500;
  padding: 14px;
  position: fixed;
  bottom: 15px;
  left: 15px;
  cursor: pointer;
`;

export { BackToConstructorButton };
